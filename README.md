Dockerized TFTP Server
----------------------

Clone and run `run_me_once.sh`.

To start container run `tftp-start.sh`.

To stop container run `tftp-stop.sh`.

Use computer IP address for an access to the tftp server.

